import { Routes } from '@angular/router';
import { ShortenerComponent } from './_components/shortener/shortener.component';

export const routes: Routes = [
  {
    path: '',
    component: ShortenerComponent,
  },
];
