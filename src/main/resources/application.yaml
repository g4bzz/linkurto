spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_linkurto?useSSL=false&serverTimezone=UTC
    username: admin
    password: senha321
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    properties:
      hibernate:
        ddl-auto: none
  application:
    name: linkurto
springdoc:
  api-docs:
    path: /api-docs